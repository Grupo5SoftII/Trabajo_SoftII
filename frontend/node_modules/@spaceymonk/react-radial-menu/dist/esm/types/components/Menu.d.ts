import React from "react";
import { MenuProps } from "./types";
import "./Menu.css";
export declare const MAIN_MENU_ID = "0";
declare const Menu: ({ centerX, centerY, innerRadius, outerRadius, animationTimeout, show, animateSubMenuChange, animation, theme, drawBackground, ...props }: MenuProps) => React.JSX.Element;
export default Menu;
